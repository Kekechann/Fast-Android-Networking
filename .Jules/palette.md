## 2026-04-22 - Adding Content Descriptions to Image Views
**Learning:** Standard Android `ImageView` elements often lack accessibility descriptions in sample apps/boilerplates.
**Action:** When updating Android UI components, ensure that `android:contentDescription` attributes are added to `ImageView` tags by defining appropriate string resources in `strings.xml`.
